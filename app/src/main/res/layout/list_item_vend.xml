<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:id="@+id/list_item_slot_view"
                xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content">
    <ImageView android:id="@+id/list_item_imageview"
               android:layout_width="wrap_content"
               android:layout_height="wrap_content"
               android:layout_gravity="center"
               android:contentDescription="@string/desc_vend_item_picture"
               android:minHeight="?android:attr/listPreferredItemHeight">
    </ImageView>
    <TextView
        android:id="@+id/list_item_price"
        style="@style/DetailShadowedText"
        android:layout_width="wrap_content"
        android:layout_height="16dp"
        android:layout_below="@+id/list_item_imageview"
        android:background="@android:color/background_light"
        android:gravity="center|bottom"
        android:text="@string/price"/>
</RelativeLayout>
